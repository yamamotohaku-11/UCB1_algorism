一度k回全部引く
nを1にする。
eachsumに結果を代入
ucbiは出た結果



for 文の中身
maxのスロットを回す。
結果をsumに足す。
結果を各スロットの結果に足す。
スロットを回した結果のUCBiを更新する
更新されたUCBiのmaxをとってUCBiのマックスを更新する。
maxのスロットの回した階数を更新する。


maxの値を入手して、リストのどの位置にいるのかを参照する必要
maxの値が重複しているときに左に寄ってしまうことを回避する
maxの値を取得してfor i range(k)ですべての値を参照して。
どのインデックスを使うかをランダムで出力する。randint(0:len(amount_max)-1)