UCB1algorismを作る。
ベータ分布を作る。
-４つのスロット、分布は手動で調整。
スロットを回したときに、分布に則って0,1を出す関数。
UCBi(t)を計算する。=mu + sqrt(log(t)/2n_i(t))。
-最初に必ず1回ずつ試行する。
最大のUCBi(t)を引く。

1.beta分布の作成。
2.１回ずつスロットを回す。
3.分布に則って値を出してUCBi(1)とする。このときリストでi番目のものがUCBi(t)[i-1]となるようにする。
4.最大のUCBi(t)[i-1]を持つi-1のスロットを引く。
5.n>4回繰り返す。繰り返す際に累積報酬の和を保持。
